<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".devicemanager.DeviceManagerFragment"
    android:orientation="vertical"
    android:background="@android:color/black">

    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/time_events_enabled_switch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="left"
        android:padding="10dp"
        android:text="@string/off_on"
        app:switchMinWidth="75dp"
        app:switchPadding="10dp" />

    <TextView
        android:id="@+id/time_events_hint"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:gravity="center"
        android:maxLines="4"
        android:padding="10dp"
        android:text="@string/time_events_hint"
        android:visibility="gone"
        app:autoSizeTextType="uniform" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/time_events_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical" />
</LinearLayout>